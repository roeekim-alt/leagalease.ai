name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
```

לחץ **Commit changes**

---

### צעד 4 — הפעל GitHub Pages

**Settings** (בתפריט העליון של ה-repo)
→ **Pages** (בתפריט השמאלי)
→ Source: שנה ל **"GitHub Actions"**
→ **Save**

---

### צעד 5 — המתן 60 שניות

לך ל-**Actions** tab — תראה run ירוק ✅

ה-URL שלך:
```
https://YOUR-USERNAME.github.io/legalease-ai
